Чтоб все было прикольно и круто и работало создайте в той же папке где docker-compose.yaml файлик `.env` и вставьте туда то что я кидал в тг
Потом `docker compose up --build`
Чтобы на фоне подрубить и использовать терминал пишите `docker compose up -d`
Чтобы выключить мультик пишите `docker compose down`
:))))