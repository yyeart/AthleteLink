from django.urls import path
from . import views

app_name = 'user'

urlpatterns = [
    # Пока оставим пустым или добавим базовые пути позже
    # path('register/', views.register, name='register'),
    # path('login/', views.user_login, name='login'),
]